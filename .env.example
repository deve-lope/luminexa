# Luminexa — see env/README.md (do not put secrets in this file)
#
# Docker (recommended):
#   cp env/docker.prod-local.example .env.docker.prod
#   cp env/docker.dev.example .env.docker.dev
#   ./scripts/docker-init-env.sh
#
# Native-only (optional):
#   cp env/native.example → split into backend/.env and frontend/.env
